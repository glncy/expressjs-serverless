<!-- Global CSS -->
<link rel='stylesheet' href='/stylesheets/style.css' />

<!-- For Custom CSS in Specific Page -->
<%if (page_id !== undefined) { %>

    <%if (page_id === "sample-page-id") { %>

        <!-- ADD CSS -->

    <% } %>

<% } %>