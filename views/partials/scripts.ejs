<!-- Global Scripts -->
<script src="https://cdn.jsdelivr.net/npm/vue/dist/vue.min.js"></script>

<!-- Local Scripts based on page-id -->
<%if (page_id !== undefined) { %>

    <%if (page_id === "sample-page-id") { %>

        <!-- ADD SCRIPTS -->

    <% } %>

<% } %>